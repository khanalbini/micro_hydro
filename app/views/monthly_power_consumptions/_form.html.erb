<%= form_for(@monthly_power_consumption) do |f| %>
  <% if @monthly_power_consumption.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@monthly_power_consumption.errors.count, "error") %> prohibited this monthly_power_consumption from being saved:</h2>

      <ul>
      <% @monthly_power_consumption.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :kwhpm %><br>
    <%= f.text_field :kwhpm %>
  </div>
  <div class="field">
    <%= f.label :month_int %><br>
    <%= f.number_field :month_int %>
  </div>
  <div class="field">
    <%= f.label :year %><br>
    <%= f.number_field :year %>
  </div>
  <div class="field">
    <%= f.label :consumer_id %><br>
    <%= f.number_field :consumer_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
